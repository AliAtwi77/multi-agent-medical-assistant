// Central place to configure the backend API base URL.
// The frontend is fully decoupled from the backend and talks to it only via HTTP.
const API_CONFIG = {
  BASE_URL: "http://localhost:8000",
};
